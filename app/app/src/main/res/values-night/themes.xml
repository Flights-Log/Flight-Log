<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark Mode Theme -->
    <style name="Base.Theme.FlightLog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="buttonStyle">@style/MyButtonStyle</item>
    </style>

    <style name="Theme.FlightLog" parent="Base.Theme.FlightLog" />

    <!-- 다크 모드 버튼 스타일 -->
    <style name="MyButtonStyle">
        <item name="android:background">@drawable/button_background</item> <!-- 다크 모드 전용 배경 -->
        <item name="android:textColor">#FFFFFF</item> <!-- 다크 모드에서는 흰색 -->
        <item name="android:textSize">15sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
